h1. Access Points

  Connect protocols to ports. Common use case is to specify Netflow ports

h2. Access Point List

div(info). 
Login as @admin@ user to manage access points.

p(autohint hand-o-right info). 
Select _Context : default > profile0 -> Access Points_


h2. Add a new access point 

div(info). 
Login as @admin@ user to manage access points.

p(autohint hand-o-right info). 
Select _Context : default > profile0 -> Access Points_

# Click the **Edit** button in the protocol list.
# Click the **Add** button to add new access point
# Enter the protocol number  and seclect the protocol.
